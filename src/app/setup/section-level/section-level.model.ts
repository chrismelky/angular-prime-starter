export class SectionLevel {
  constructor(
    public id?: number,
    public code?: string,
    public name?: string,
    public position?: number,
    public code_required?: boolean,
    public is_cost_centre?: boolean,
    public code_length?: number
  ) {}
}
